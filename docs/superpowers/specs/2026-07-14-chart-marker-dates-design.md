# 同日买卖标记与日期轴显示设计

## 目标

让同一交易日的买入和卖出标记在图表中可同时辨认，并让横坐标刻度显示完整日期。

## 方案

保持后端交易、收益和行情计算不变。在浏览器端，将同一日期的买卖标记按成交方向分别偏移到该日的前后半天；悬停内容仍使用原始成交日期。横坐标继续使用时间轴，但以 `YYYY-MM-DD` 格式显示刻度。

## 范围与验证

- 同日买入、卖出分别拥有不同的图表坐标，两个点均可见。
- 其他交易日的标记仍在其实际交易日位置。
- 横坐标标签显示年月日，不显示仅月份的简写。
- 新增浏览器端单元测试，并运行全部 Python 测试。
